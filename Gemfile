source :rubygems

gem 'thin'
gem 'rails', '3.1.0.rc5'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'
  gem 'uglifier'
end

gem 'haml'
gem 'jquery-rails'

group :development do
  gem 'heroku'
  gem 'foreman'
end
